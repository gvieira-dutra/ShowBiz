@model IEnumerable<GVD2247A5.Models.ShowBaseViewModel>

@{
    ViewBag.Title = "Show List";
}

<h2>Show List</h2>


@*@using System.Security.Claims;
@if ((User as ClaimsPrincipal).HasClaim(ClaimTypes.Role, "Coordinator"))
{
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
}*@
<hr />
@if (Model.Count() > 0) { 
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Genre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReleaseDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ImageUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Coordinator)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Genre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReleaseDate)
                </td>
                <td>
                    <img src="@item.ImageUrl" alt="@item.Name" style=" max-width: 100px; max-height: 100px" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Coordinator)
                </td>
                <td>
                    @Html.ActionLink("Details", "Details", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
} else { <p>There are no shows yet</p>}
